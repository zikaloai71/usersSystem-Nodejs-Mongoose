{{>header}}
{{>nav}}
<form  method="post" class="container my-3">
  <div class="form-group">
    <input class="form-control my-1" value="{{address.addressType}}" name='addressType'  placeholder="Enter address Type">
  </div>
   <div class="form-group">
    <input  class="form-control my-1" value="{{address.addressDetail}}" name='addressDetail'  placeholder="Enter addressDetail">
  </div>
  
  <button type="submit" class="btn btn-primary">edit address</button>
</form>
{{>footer}}